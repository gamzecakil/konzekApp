
    <h1>Giriş Ekranı</h1>
    <%  if(messages.error){%>
    <%= messages.error%>
    <%}%>
    <form action="/login" method="POST">
        <div>
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required/>(emal:gmzcakil@gmail.com)
        </div>
        <div>
            <label for="password">Şifre</label>
            <input type="password" id="password" name="password" required />(pass:123456)
        </div>
        <button type="sumbit">Giriş Yap</button>
    </form>
